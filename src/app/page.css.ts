import { style } from "@vanilla-extract/css";

export const hero_wrapper = style({
    height: "100dvh",
    width: "100%",
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
})